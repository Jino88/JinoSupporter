Review index reference:
RequestJson={{requestPath}}
ReviewIndexHtml={{detailedReviewIndexPath}}

- Open RequestJson.reviewIndex.indexHtmlPath when RequestJson.reviewIndex.exists
  is true, but treat it as a taxonomy/reference table generated by a previous
  AI run.
- This applies to INPUT DATA (TEST) and INPUT DATA (BATCH). For BATCH, use this
  HTML after mandatory workbook-text AI pre-analysis, never instead of it.
- Search RequestJson.reviewIndex.matchKeys first. If matchKeys is absent, use
  workbook.fileName and workbook.sourceDataset only as search keys.
- Try exact text match, then case-insensitive normalized filename/dataset match.
- Use the matched row's purpose, target defect, review item, model, date, and
  confidence only to align wording or compare against workbook-text
  classification.
- The review index is not evidence for the workbook result. It cannot prove
  which condition improved/worsened, what chart to use, or whether the report is
  process-side or function-side.
- If the matched row conflicts with Step 1 extracted workbook evidence, state
  the conflict and prioritize workbook evidence.
- If no matching row is found, say briefly that the review index had no matching
  row and continue from workbook evidence.
