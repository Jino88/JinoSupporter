Translate every value in this JSON to {{targetLanguage}}. Keep keys unchanged. Preserve numbers, units, and product/part identifiers verbatim. If a field is empty, return it as an empty string.

"actionTexts" is an array of strings. Translate each element and return an array of the same length and order.

{{scriptRules}}

Output ONLY the translated JSON object. No markdown fence, no commentary, no extra keys.

Input:
{{inputJson}}
