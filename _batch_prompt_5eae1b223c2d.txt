AI_EXCEL_PROC.md 읽고 AI Batch 실행해. DB=D:\000. MyWorks\002. DB\process-review.db. Targets=_batch_selected_20260514_085544_428d40e_2.txt (315개). Mode=Reanalyze. IMPORTANT: _batch_selected_20260514_085544_428d40e_2.txt is a newline-delimited target list with exactly 315 dataset names. Read the full file first, then process EVERY non-empty line. Do not stop after the first dataset. Continue line by line until all 315 targets are completed or explicitly logged as failed. Before finishing, verify processed_count + failed_count == 315. Delete _batch_selected_20260514_085544_428d40e_2.txt only after that verification passes. Use _ai_batch_helper.get_excel_text(con, dataset) first. It renders every attached workbook and every worksheet in workbook order with sheet names, cell coordinates, merged ranges, styles, formulas, and dimensions preserved. Process EVERY worksheet, not only the first visible sheet. If the rendered text is too large, split by '=== SHEET:' and analyze sheets sequentially, then commit one combined result. If you need workbook paths, use _ai_batch_helper.get_excel_files(con, dataset). Treat those text-only workbook files as the source of truth; do not flatten them to TSV/openpyxl values as the authoritative input. Use get_excel_paste only as last-resort fallback. Store AiResults/AiNgBreakdowns from all relevant sheets and set sheet_name on every extracted row so Dataset Results can prove sheet coverage. If excel_paste contains metadata tags such as {merged=A1:A4} or 〔merged=A1:A4〕, strip the tag and use the following value as the cell value. Merged-cell rule: if Date/Model/Type/Line/Process cells are merged or visually carried across rows, all covered rows inherit that value before storing AiResults and before pairing Test/Normal rows. Do not create standalone AiResults from percentage-only subrows; attach them as breakdown/rate evidence to the preceding count row. Set document.report_type to exactly one of normal_comparison, ng_without_baseline, before_after_dimension, measurement_spec, defect_root_cause, lot_supplier_mold_comparison, process_condition_change, reliability_spec, doe_matrix, image_dependent, mixed. Extract enough fields for a per-workbook mini report: basic info, problem/phenomenon, purpose, changed/tested conditions, result data, Normal/Test or Before/After comparison, writer decision/note, AI judgement/warnings, and evidence location. Keep narrative short and table-ready. If no same-event baseline exists, do not suppress tables and do not claim improvement/worsening; use ng_without_baseline ranking instead. For dimension/reliability/DOE/image-dependent reports, store metric rows, judgement, condition labels, and source sheets so DatasetResult can render the matching dashboard. 스키마는 AiDocuments/AiTestConditions/AiResults/AiNgBreakdowns/AiConclusions/AiTroubleshootingHints/AiExtractionLogs 사용. 분석/결론은 절대 불량률 순위가 아니라 같은 시트/같은 시험 이벤트의 Normal/Baseline/Before/Old/OK 대조군 대비 상대 변화율((test/base-1)*100, 몇% 개선/악화) 기준으로 판단. narrative 필드 (title/purpose/content, conclusions.statement_from_report/normalized_interpretation, troubleshooting.check_item/reason, ai_extraction_log.decision_rationale/assumptions/warnings) 는 AiDocumentTranslations/AiConclusionTranslations/AiHintTranslations/AiLogTranslations 에 Lang ∈ {ko,en,vi} 3개 모두 저장. 미처리 쿼리 무시하고 targets 만 처리, 끝나면 _batch_selected_20260514_085544_428d40e_2.txt 삭제.