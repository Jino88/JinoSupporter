<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:controls="clr-namespace:JinoSupporter.Controls">

    <!-- ================================================================
         Color Tokens
         ================================================================ -->
    <Color x:Key="AppSurfaceColor">#FFFFFF</Color>
    <Color x:Key="AppBackgroundColor">#F4F7FB</Color>
    <Color x:Key="AppBorderColor">#B8C7DB</Color>
    <Color x:Key="AppAccentColor">#0A63C9</Color>
    <Color x:Key="AppAccentHoverColor">#094FA1</Color>
    <Color x:Key="AppTextPrimaryColor">#1D2733</Color>
    <Color x:Key="AppTextSecondaryColor">#46586E</Color>
    <Color x:Key="AppAccentAltColor">#4F6BED</Color>
    <Color x:Key="AppAccentAltHoverColor">#3D5AE0</Color>
    <Color x:Key="AppSuccessColor">#16A34A</Color>
    <Color x:Key="AppSuccessHoverColor">#158040</Color>
    <Color x:Key="AppDangerColor">#EF4444</Color>
    <Color x:Key="AppDangerHoverColor">#DC2626</Color>
    <Color x:Key="AppGhostHoverColor">#EEF3F8</Color>

    <!-- ================================================================
         Brush Tokens
         ================================================================ -->
    <SolidColorBrush x:Key="AppSurfaceBrush"          Color="{StaticResource AppSurfaceColor}" />
    <SolidColorBrush x:Key="AppBackgroundBrush"        Color="{StaticResource AppBackgroundColor}" />
    <SolidColorBrush x:Key="AppBorderBrush"            Color="{StaticResource AppBorderColor}" />
    <SolidColorBrush x:Key="AppAccentBrush"            Color="{StaticResource AppAccentColor}" />
    <SolidColorBrush x:Key="AppAccentHoverBrush"       Color="{StaticResource AppAccentHoverColor}" />
    <SolidColorBrush x:Key="AppTextPrimaryBrush"       Color="{StaticResource AppTextPrimaryColor}" />
    <SolidColorBrush x:Key="AppTextSecondaryBrush"     Color="{StaticResource AppTextSecondaryColor}" />
    <SolidColorBrush x:Key="AppAccentAltBrush"         Color="{StaticResource AppAccentAltColor}" />
    <SolidColorBrush x:Key="AppAccentAltHoverBrush"    Color="{StaticResource AppAccentAltHoverColor}" />
    <SolidColorBrush x:Key="AppSuccessBrush"           Color="{StaticResource AppSuccessColor}" />
    <SolidColorBrush x:Key="AppSuccessHoverBrush"      Color="{StaticResource AppSuccessHoverColor}" />
    <SolidColorBrush x:Key="AppDangerBrush"            Color="{StaticResource AppDangerColor}" />
    <SolidColorBrush x:Key="AppDangerHoverBrush"       Color="{StaticResource AppDangerHoverColor}" />
    <SolidColorBrush x:Key="AppGhostHoverBrush"        Color="{StaticResource AppGhostHoverColor}" />

    <!-- ================================================================
         Helper: ComboBox 드롭다운 화살표 ToggleButton (내부 전용)
         ================================================================ -->
    <Style x:Key="AppComboBoxInternalToggleStyle" TargetType="{x:Type ToggleButton}">
        <Setter Property="Focusable"  Value="False" />
        <Setter Property="ClickMode"  Value="Press" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ToggleButton}">
                    <Border Background="Transparent" BorderThickness="0">
                        <Path HorizontalAlignment="Center"
                              VerticalAlignment="Center"
                              Data="M 0 0 L 4 4 L 8 0 Z"
                              Fill="{StaticResource AppTextSecondaryBrush}"
                              SnapsToDevicePixels="True" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Opacity" Value="0.8" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.45" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ================================================================
         AppButton — Base Style (x:Key로 외부에서 BasedOn 가능)
         ================================================================ -->
    <Style x:Key="AppButtonBaseStyle" TargetType="{x:Type controls:AppButton}">
        <Setter Property="MinHeight"                  Value="28" />
        <Setter Property="Padding"                    Value="10,5" />
        <Setter Property="Margin"                     Value="0" />
        <Setter Property="Background"                 Value="#FFFFFF" />
        <Setter Property="BorderBrush"                Value="#D5DFEC" />
        <Setter Property="BorderThickness"            Value="1" />
        <Setter Property="Foreground"                 Value="{StaticResource AppTextPrimaryBrush}" />
        <Setter Property="FontSize"                   Value="10.5" />
        <Setter Property="FontWeight"                 Value="SemiBold" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="VerticalContentAlignment"   Value="Center" />
        <Setter Property="Cursor"                     Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:AppButton}">
                    <Border x:Name="ButtonBorder"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="6"
                            SnapsToDevicePixels="True">
                        <ContentPresenter Margin="{TemplateBinding Padding}"
                                          HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                          RecognizesAccessKey="True" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="ButtonBorder" Property="Background"  Value="#F7FAFD" />
                            <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="#B7C8DE" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="ButtonBorder" Property="Background"  Value="#EEF3F8" />
                            <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="#9EB4CF" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.55" />
                            <Setter Property="Cursor"  Value="Arrow" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <!-- Accent -->
            <Trigger Property="ButtonType" Value="Accent">
                <Setter Property="Background"  Value="{StaticResource AppAccentAltBrush}" />
                <Setter Property="BorderBrush" Value="{StaticResource AppAccentAltBrush}" />
                <Setter Property="Foreground"  Value="White" />
            </Trigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="ButtonType"  Value="Accent" />
                    <Condition Property="IsMouseOver" Value="True" />
                </MultiTrigger.Conditions>
                <Setter Property="Background"  Value="{StaticResource AppAccentAltHoverBrush}" />
                <Setter Property="BorderBrush" Value="{StaticResource AppAccentAltHoverBrush}" />
            </MultiTrigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="ButtonType" Value="Accent" />
                    <Condition Property="IsPressed"  Value="True" />
                </MultiTrigger.Conditions>
                <Setter Property="Background"  Value="#2E4BD3" />
                <Setter Property="BorderBrush" Value="#2E4BD3" />
            </MultiTrigger>
            <!-- Danger -->
            <Trigger Property="ButtonType" Value="Danger">
                <Setter Property="Background"  Value="{StaticResource AppDangerBrush}" />
                <Setter Property="BorderBrush" Value="{StaticResource AppDangerBrush}" />
                <Setter Property="Foreground"  Value="White" />
            </Trigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="ButtonType"  Value="Danger" />
                    <Condition Property="IsMouseOver" Value="True" />
                </MultiTrigger.Conditions>
                <Setter Property="Background"  Value="{StaticResource AppDangerHoverBrush}" />
                <Setter Property="BorderBrush" Value="{StaticResource AppDangerHoverBrush}" />
            </MultiTrigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="ButtonType" Value="Danger" />
                    <Condition Property="IsPressed"  Value="True" />
                </MultiTrigger.Conditions>
                <Setter Property="Background"  Value="#B91C1C" />
                <Setter Property="BorderBrush" Value="#B91C1C" />
            </MultiTrigger>
            <!-- Success -->
            <Trigger Property="ButtonType" Value="Success">
                <Setter Property="Background"  Value="{StaticResource AppSuccessBrush}" />
                <Setter Property="BorderBrush" Value="{StaticResource AppSuccessBrush}" />
                <Setter Property="Foreground"  Value="White" />
            </Trigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="ButtonType"  Value="Success" />
                    <Condition Property="IsMouseOver" Value="True" />
                </MultiTrigger.Conditions>
                <Setter Property="Background"  Value="{StaticResource AppSuccessHoverBrush}" />
                <Setter Property="BorderBrush" Value="{StaticResource AppSuccessHoverBrush}" />
            </MultiTrigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="ButtonType" Value="Success" />
                    <Condition Property="IsPressed"  Value="True" />
                </MultiTrigger.Conditions>
                <Setter Property="Background"  Value="#116636" />
                <Setter Property="BorderBrush" Value="#116636" />
            </MultiTrigger>
            <!-- Ghost -->
            <Trigger Property="ButtonType" Value="Ghost">
                <Setter Property="Background"  Value="Transparent" />
                <Setter Property="BorderBrush" Value="Transparent" />
                <Setter Property="Foreground"  Value="{StaticResource AppTextPrimaryBrush}" />
            </Trigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="ButtonType"  Value="Ghost" />
                    <Condition Property="IsMouseOver" Value="True" />
                </MultiTrigger.Conditions>
                <Setter Property="Background"  Value="{StaticResource AppGhostHoverBrush}" />
                <Setter Property="BorderBrush" Value="Transparent" />
            </MultiTrigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="ButtonType" Value="Ghost" />
                    <Condition Property="IsPressed"  Value="True" />
                </MultiTrigger.Conditions>
                <Setter Property="Background"  Value="#E4EBF3" />
                <Setter Property="BorderBrush" Value="Transparent" />
            </MultiTrigger>
        </Style.Triggers>
    </Style>

    <!-- AppButton — DefaultStyleKey 연결 (BasedOn Base) -->
    <Style TargetType="{x:Type controls:AppButton}"
           BasedOn="{StaticResource AppButtonBaseStyle}" />

    <!-- ================================================================
         AppTextBox — Base Style
         ================================================================ -->
    <Style x:Key="AppTextBoxBaseStyle" TargetType="{x:Type controls:AppTextBox}">
        <Setter Property="MinHeight"              Value="28" />
        <Setter Property="Padding"                Value="6,4" />
        <Setter Property="Foreground"             Value="{StaticResource AppTextPrimaryBrush}" />
        <Setter Property="Background"             Value="{StaticResource AppSurfaceBrush}" />
        <Setter Property="BorderBrush"            Value="{StaticResource AppBorderBrush}" />
        <Setter Property="BorderThickness"        Value="1" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="VerticalAlignment"      Value="Center" />
        <Setter Property="FontSize"               Value="10.5" />
        <Setter Property="CaretBrush"             Value="{StaticResource AppTextPrimaryBrush}" />
        <Setter Property="SelectionBrush"         Value="{StaticResource AppAccentBrush}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:AppTextBox}">
                    <Border x:Name="TextBoxBorder"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="6"
                            SnapsToDevicePixels="True">
                        <Grid>
                            <ScrollViewer x:Name="PART_ContentHost"
                                          Margin="{TemplateBinding Padding}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                          Focusable="False" />
                            <TextBlock x:Name="PlaceholderText"
                                       Margin="{TemplateBinding Padding}"
                                       VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                       Foreground="{StaticResource AppTextSecondaryBrush}"
                                       Text="{TemplateBinding Placeholder}"
                                       FontSize="{TemplateBinding FontSize}"
                                       IsHitTestVisible="False"
                                       Visibility="Collapsed" />
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="Text"      Value="" />
                                <Condition Property="IsFocused" Value="False" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="PlaceholderText" Property="Visibility" Value="Visible" />
                        </MultiTrigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="TextBoxBorder" Property="BorderBrush" Value="#96AEC9" />
                        </Trigger>
                        <Trigger Property="IsKeyboardFocused" Value="True">
                            <Setter TargetName="TextBoxBorder" Property="BorderBrush" Value="{StaticResource AppAccentBrush}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.55" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- AppTextBox — DefaultStyleKey 연결 -->
    <Style TargetType="{x:Type controls:AppTextBox}"
           BasedOn="{StaticResource AppTextBoxBaseStyle}" />

    <!-- ================================================================
         AppComboBox — Base Style
         ================================================================ -->
    <Style x:Key="AppComboBoxBaseStyle" TargetType="{x:Type controls:AppComboBox}">
        <Setter Property="Height"                 Value="28" />
        <Setter Property="MinHeight"              Value="28" />
        <Setter Property="Padding"                Value="6,1" />
        <Setter Property="Margin"                 Value="0" />
        <Setter Property="Foreground"             Value="{StaticResource AppTextPrimaryBrush}" />
        <Setter Property="Background"             Value="{StaticResource AppSurfaceBrush}" />
        <Setter Property="BorderBrush"            Value="{StaticResource AppBorderBrush}" />
        <Setter Property="BorderThickness"        Value="1" />
        <Setter Property="VerticalContentAlignment"   Value="Center" />
        <Setter Property="VerticalAlignment"          Value="Center" />
        <Setter Property="HorizontalContentAlignment" Value="Left" />
        <Setter Property="FontSize"               Value="10.5" />
        <Setter Property="SnapsToDevicePixels"    Value="True" />
        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto" />
        <Setter Property="ScrollViewer.VerticalScrollBarVisibility"   Value="Auto" />
        <Setter Property="ScrollViewer.CanContentScroll"              Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:AppComboBox}">
                    <Grid>
                        <Border x:Name="OuterBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6"
                                SnapsToDevicePixels="True" />
                        <Grid Margin="1">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="20" />
                            </Grid.ColumnDefinitions>
                            <ContentPresenter x:Name="ContentSite"
                                              Grid.Column="0"
                                              Margin="{TemplateBinding Padding}"
                                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                              Content="{TemplateBinding SelectionBoxItem}"
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                              ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                              IsHitTestVisible="False" />
                            <TextBox x:Name="PART_EditableTextBox"
                                     Grid.Column="0"
                                     Margin="{TemplateBinding Padding}"
                                     HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                                     VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}"
                                     Background="Transparent"
                                     BorderThickness="0"
                                     Foreground="{TemplateBinding Foreground}"
                                     Focusable="True"
                                     Visibility="Hidden" />
                            <ToggleButton Grid.Column="1"
                                          Background="Transparent"
                                          BorderBrush="Transparent"
                                          BorderThickness="0"
                                          Focusable="False"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                          Style="{StaticResource AppComboBoxInternalToggleStyle}" />
                        </Grid>
                        <Popup x:Name="Popup"
                               Placement="Bottom"
                               AllowsTransparency="True"
                               Focusable="False"
                               IsOpen="{TemplateBinding IsDropDownOpen}"
                               PopupAnimation="Slide">
                            <Grid MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}"
                                  MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                <Border Background="{StaticResource AppSurfaceBrush}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="1"
                                        CornerRadius="5" />
                                <ScrollViewer Margin="2" SnapsToDevicePixels="True">
                                    <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Contained" />
                                </ScrollViewer>
                            </Grid>
                        </Popup>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="HasItems" Value="False">
                            <Setter TargetName="Popup" Property="MinHeight" Value="24" />
                        </Trigger>
                        <Trigger Property="IsEditable" Value="True">
                            <Setter Property="IsTabStop" Value="False" />
                            <Setter TargetName="PART_EditableTextBox" Property="Visibility" Value="Visible" />
                            <Setter TargetName="ContentSite"           Property="Visibility" Value="Hidden" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="OuterBorder" Property="BorderBrush" Value="#96AEC9" />
                            <Setter TargetName="OuterBorder" Property="Background"  Value="#FCFDFE" />
                        </Trigger>
                        <Trigger Property="IsKeyboardFocusWithin" Value="True">
                            <Setter TargetName="OuterBorder" Property="BorderBrush" Value="{StaticResource AppAccentBrush}" />
                        </Trigger>
                        <Trigger Property="IsDropDownOpen" Value="True">
                            <Setter TargetName="OuterBorder" Property="BorderBrush" Value="{StaticResource AppAccentBrush}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.55" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- AppComboBox — DefaultStyleKey 연결 -->
    <Style TargetType="{x:Type controls:AppComboBox}"
           BasedOn="{StaticResource AppComboBoxBaseStyle}" />

    <!-- ================================================================
         AppListBox — Base Style
         ================================================================ -->
    <Style x:Key="AppListBoxBaseStyle" TargetType="{x:Type controls:AppListBox}">
        <Setter Property="Background"      Value="{StaticResource AppSurfaceBrush}" />
        <Setter Property="BorderBrush"     Value="{StaticResource AppBorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding"         Value="4" />
        <Setter Property="FontSize"        Value="10.5" />
        <Setter Property="Foreground"      Value="{StaticResource AppTextPrimaryBrush}" />
    </Style>

    <!-- AppListBox — DefaultStyleKey 연결 -->
    <Style TargetType="{x:Type controls:AppListBox}"
           BasedOn="{StaticResource AppListBoxBaseStyle}" />

    <!-- ================================================================
         AppDataGrid — Base Style
         ================================================================ -->
    <Style x:Key="AppDataGridBaseStyle" TargetType="{x:Type controls:AppDataGrid}">
        <Setter Property="Background"               Value="{StaticResource AppSurfaceBrush}" />
        <Setter Property="BorderBrush"              Value="{StaticResource AppBorderBrush}" />
        <Setter Property="BorderThickness"          Value="1" />
        <Setter Property="GridLinesVisibility"      Value="Horizontal" />
        <Setter Property="HorizontalGridLinesBrush" Value="#D6E0ED" />
        <Setter Property="VerticalGridLinesBrush"   Value="#E2EAF5" />
        <Setter Property="HeadersVisibility"        Value="All" />
        <Setter Property="AlternationCount"         Value="2" />
        <Setter Property="RowBackground"            Value="#FFFFFF" />
        <Setter Property="AlternatingRowBackground" Value="#F7FAFD" />
        <Setter Property="FontSize"                 Value="10.5" />
        <Setter Property="Foreground"               Value="{StaticResource AppTextPrimaryBrush}" />
        <Setter Property="RowHeight"                Value="26" />
        <Setter Property="ColumnHeaderHeight"       Value="28" />
        <Setter Property="AutoGenerateColumns"      Value="False" />
        <Setter Property="CanUserAddRows"           Value="False" />
        <Setter Property="CanUserDeleteRows"        Value="False" />
        <Setter Property="SelectionMode"            Value="Single" />
    </Style>

    <!-- AppDataGrid — DefaultStyleKey 연결 -->
    <Style TargetType="{x:Type controls:AppDataGrid}"
           BasedOn="{StaticResource AppDataGridBaseStyle}" />

    <!-- ================================================================
         AppProgressBar — Base Style
         ================================================================ -->
    <Style x:Key="AppProgressBarBaseStyle" TargetType="{x:Type controls:AppProgressBar}">
        <Setter Property="Height"          Value="5" />
        <Setter Property="Background"      Value="#F0F1F5" />
        <Setter Property="Foreground"      Value="{StaticResource AppAccentAltBrush}" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:AppProgressBar}">
                    <Grid>
                        <Border Background="{TemplateBinding Background}" CornerRadius="3" />
                        <Border x:Name="PART_Track" />
                        <Border x:Name="PART_Indicator"
                                HorizontalAlignment="Left"
                                Background="{TemplateBinding Foreground}"
                                CornerRadius="3" />
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- AppProgressBar — DefaultStyleKey 연결 -->
    <Style TargetType="{x:Type controls:AppProgressBar}"
           BasedOn="{StaticResource AppProgressBarBaseStyle}" />

    <!-- ================================================================
         AppCheckBox — Base Style
         ================================================================ -->
    <Style x:Key="AppCheckBoxBaseStyle" TargetType="{x:Type controls:AppCheckBox}">
        <Setter Property="VerticalAlignment"        Value="Center" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Margin"                   Value="2,2" />
        <Setter Property="Foreground"               Value="{StaticResource AppTextPrimaryBrush}" />
        <Setter Property="FontSize"                 Value="10.5" />
        <Setter Property="Cursor"                   Value="Hand" />
    </Style>

    <!-- AppCheckBox — DefaultStyleKey 연결 -->
    <Style TargetType="{x:Type controls:AppCheckBox}"
           BasedOn="{StaticResource AppCheckBoxBaseStyle}" />

    <!-- ================================================================
         AppToggleButton — Base Style
         ================================================================ -->
    <Style x:Key="AppToggleButtonBaseStyle" TargetType="{x:Type controls:AppToggleButton}">
        <Setter Property="MinHeight"                  Value="28" />
        <Setter Property="MinWidth"                   Value="72" />
        <Setter Property="Padding"                    Value="10,5" />
        <Setter Property="Margin"                     Value="0" />
        <Setter Property="Background"                 Value="#FFFFFF" />
        <Setter Property="BorderBrush"                Value="#D5DFEC" />
        <Setter Property="BorderThickness"            Value="1" />
        <Setter Property="Foreground"                 Value="{StaticResource AppTextPrimaryBrush}" />
        <Setter Property="FontSize"                   Value="10.5" />
        <Setter Property="FontWeight"                 Value="SemiBold" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="VerticalContentAlignment"   Value="Center" />
        <Setter Property="Cursor"                     Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:AppToggleButton}">
                    <Border x:Name="ToggleBorder"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="6"
                            SnapsToDevicePixels="True">
                        <ContentPresenter Margin="{TemplateBinding Padding}"
                                          HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                          RecognizesAccessKey="True" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="ToggleBorder" Property="Background"  Value="{StaticResource AppAccentAltBrush}" />
                            <Setter TargetName="ToggleBorder" Property="BorderBrush" Value="{StaticResource AppAccentAltBrush}" />
                            <Setter Property="Foreground" Value="White" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="ToggleBorder" Property="Background"  Value="#F7FAFD" />
                            <Setter TargetName="ToggleBorder" Property="BorderBrush" Value="#B7C8DE" />
                        </Trigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsChecked"   Value="True" />
                                <Condition Property="IsMouseOver" Value="True" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="ToggleBorder" Property="Background"  Value="{StaticResource AppAccentAltHoverBrush}" />
                            <Setter TargetName="ToggleBorder" Property="BorderBrush" Value="{StaticResource AppAccentAltHoverBrush}" />
                        </MultiTrigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="ToggleBorder" Property="Background"  Value="#EEF3F8" />
                            <Setter TargetName="ToggleBorder" Property="BorderBrush" Value="#9EB4CF" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.55" />
                            <Setter Property="Cursor"  Value="Arrow" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- AppToggleButton — DefaultStyleKey 연결 -->
    <Style TargetType="{x:Type controls:AppToggleButton}"
           BasedOn="{StaticResource AppToggleButtonBaseStyle}" />

    <!-- ================================================================
         AppCardBorder — Base Style
         ================================================================ -->
    <Style x:Key="AppCardBorderBaseStyle" TargetType="{x:Type controls:AppCardBorder}">
        <Setter Property="Background"          Value="{StaticResource AppSurfaceBrush}" />
        <Setter Property="BorderBrush"         Value="{StaticResource AppBorderBrush}" />
        <Setter Property="BorderThickness"     Value="1" />
        <Setter Property="CornerRadius"        Value="6" />
        <Setter Property="Padding"             Value="10" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
    </Style>

    <!-- AppCardBorder — DefaultStyleKey 연결 -->
    <Style TargetType="{x:Type controls:AppCardBorder}"
           BasedOn="{StaticResource AppCardBorderBaseStyle}" />

</ResourceDictionary>
