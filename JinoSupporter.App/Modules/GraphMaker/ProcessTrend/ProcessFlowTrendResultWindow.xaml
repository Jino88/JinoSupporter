<Window x:Class="GraphMaker.ProcessFlowTrendResultWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:app="clr-namespace:JinoSupporter.Controls;assembly=JinoSupporter.Controls"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:oxy="http://oxyplot.org/wpf"
        mc:Ignorable="d"
        Title="Process Trend Result"
        Width="1400"
        Height="900"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="12">
        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <ItemsControl ItemsSource="{Binding PairResults}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Width="430"
                                Height="340"
                                Margin="6"
                                Padding="8"
                                BorderBrush="#D0D7DE"
                                BorderThickness="1"
                                CornerRadius="6"
                                Background="White">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="2*" />
                                    <RowDefinition Height="*" />
                                </Grid.RowDefinitions>

                                <TextBlock Text="{Binding PairTitle}"
                                           FontWeight="SemiBold"
                                           Margin="0,0,0,6"
                                           Cursor="Hand"
                                           TextDecorations="Underline"
                                           ToolTip="Click to open large view"
                                           MouseLeftButtonUp="PairTitle_MouseLeftButtonUp" />

                                <Border Grid.Row="1" BorderBrush="#E5E7EB" BorderThickness="1">
                                    <oxy:PlotView Model="{Binding PlotModel}" />
                                </Border>

                                <app:AppTextBox Grid.Row="2"
                                         Margin="0,8,0,0"
                                         Text="{Binding DetailText}"
                                         IsReadOnly="True"
                                         VerticalScrollBarVisibility="Auto"
                                         HorizontalScrollBarVisibility="Auto"
                                         TextWrapping="Wrap"
                                         FontFamily="Consolas"
                                         FontSize="12" />
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</Window>
