<Window x:Class="GraphMaker.GraphViewerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:app="clr-namespace:JinoSupporter.Controls;assembly=JinoSupporter.Controls"
        xmlns:oxy="http://oxyplot.org/wpf"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="SPL Scatter Graph"
        Height="900"
        Width="1600"
        MinHeight="760"
        MinWidth="1280"
        WindowStartupLocation="CenterScreen"
        WindowState="Maximized">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <GroupBox Grid.Row="0"
                  Grid.Column="0"
                  Margin="0,0,8,8">
            <GroupBox.Header>
                <TextBlock Text="Line Graph (with Std, Upper, Lower)"
                           Cursor="Hand"
                           TextDecorations="Underline"
                           ToolTip="Click to open large view"
                           MouseLeftButtonUp="LineHeader_MouseLeftButtonUp" />
            </GroupBox.Header>
            <oxy:PlotView x:Name="LineChartView" Model="{Binding LineChartModel}" />
        </GroupBox>

        <GroupBox Grid.Row="0"
                  Grid.Column="1"
                  Margin="8,0,0,8">
            <GroupBox.Header>
                <TextBlock Text="Normal Distribution"
                           Cursor="Hand"
                           TextDecorations="Underline"
                           ToolTip="Click to open large view"
                           MouseLeftButtonUp="ScatterHeader_MouseLeftButtonUp" />
            </GroupBox.Header>
            <oxy:PlotView x:Name="ScatterChartView" Model="{Binding ScatterChartModel}" />
        </GroupBox>

        <GroupBox Grid.Row="1"
                  Grid.Column="0"
                  Margin="0,8,8,0">
            <GroupBox.Header>
                <TextBlock Text="Average by X (excluding Std/Upper/Lower)"
                           Cursor="Hand"
                           TextDecorations="Underline"
                           ToolTip="Click to open large view"
                           MouseLeftButtonUp="AverageHeader_MouseLeftButtonUp" />
            </GroupBox.Header>
            <oxy:PlotView x:Name="AverageChartView" Model="{Binding AverageChartModel}" />
        </GroupBox>

        <GroupBox Grid.Row="1"
                  Grid.Column="1"
                  Margin="8,8,0,0">
            <GroupBox.Header>
                <TextBlock Text="CPK by X (excluding Std/Upper/Lower)"
                           Cursor="Hand"
                           TextDecorations="Underline"
                           ToolTip="Click to open large view"
                           MouseLeftButtonUp="CpkHeader_MouseLeftButtonUp" />
            </GroupBox.Header>
            <oxy:PlotView x:Name="CpkChartView" Model="{Binding CpkChartModel}" />
        </GroupBox>
    </Grid>
</Window>

