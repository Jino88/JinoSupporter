<Window x:Class="WorkbenchHost.Modules.ScreenCapture.RegionCaptureOverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:app="clr-namespace:JinoSupporter.Controls;assembly=JinoSupporter.Controls"
        WindowStyle="None"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        AllowsTransparency="True"
        Background="#22000000"
        Topmost="True"
        Cursor="Cross"
        KeyDown="Window_KeyDown"
        MouseLeftButtonDown="Window_MouseLeftButtonDown"
        MouseMove="Window_MouseMove"
        MouseLeftButtonUp="Window_MouseLeftButtonUp">
    <Canvas x:Name="RootCanvas">
        <Rectangle x:Name="SelectionBorder"
                   Stroke="#FFF3D26A"
                   StrokeThickness="2"
                   Fill="#33FFF1A6"
                   Visibility="Collapsed" />
        <Border Background="#CC111827"
                CornerRadius="8"
                Padding="12,8"
                Canvas.Left="24"
                Canvas.Top="24">
            <StackPanel>
                <TextBlock Foreground="White"
                           FontSize="14"
                           FontWeight="SemiBold"
                           Text="Capture Region" />
                <TextBlock Margin="0,4,0,0"
                           Foreground="#D7E0EC"
                           FontSize="11"
                           Text="Drag to select an area. Press Esc to cancel." />
            </StackPanel>
        </Border>
    </Canvas>
</Window>
