<Window x:Class="DataMaker.TableRowInputWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add Table Row"
        Width="1480"
        Height="920"
        MinWidth="1240"
        MinHeight="760"
        MaxHeight="1080"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        Background="#F5F6FA">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="HeaderTextBlock"
                   FontSize="18"
                   FontWeight="SemiBold"
                   Foreground="#1A1D26"/>

        <TextBlock x:Name="PathTextBlock"
                   Grid.Row="1"
                   Margin="0,8,0,12"
                   TextWrapping="Wrap"
                   Foreground="#6B7280"/>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.2*"/>
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0"
                    Background="White"
                    BorderBrush="#E2E4EA"
                    BorderThickness="1"
                    CornerRadius="8"
                    Padding="12">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock x:Name="MissingHeaderTextBlock"
                               Text="Unregistered Items"
                               FontSize="14"
                               FontWeight="SemiBold"
                               Foreground="#1A1D26"
                               Margin="0,0,0,10"/>

                    <DataGrid x:Name="MissingItemsDataGrid"
                              Grid.Row="1"
                              SelectionMode="Single"
                              SelectionUnit="FullRow"
                              IsReadOnly="True"
                              AutoGenerateColumns="True"
                              CanUserAddRows="False"
                              CanUserDeleteRows="False"
                              CanUserResizeRows="False"
                              HeadersVisibility="Column"
                              GridLinesVisibility="Horizontal"
                              SelectionChanged="MissingItemsDataGrid_SelectionChanged"
                              Background="Transparent"
                              BorderThickness="0"/>
                </Grid>
            </Border>

            <ScrollViewer Grid.Column="2"
                          VerticalScrollBarVisibility="Auto">
                <StackPanel x:Name="FieldsPanel"/>
            </ScrollViewer>
        </Grid>

        <StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,16,0,0">
            <Button Content="Cancel"
                    Width="96"
                    Margin="0,0,8,0"
                    Click="CancelButton_Click"
                    Background="#F0F1F5"
                    Foreground="#1A1D26"
                    BorderBrush="#E2E4EA"
                    BorderThickness="1"
                    Padding="0,8"/>
            <Button Content="Add"
                    Width="96"
                    Click="SaveButton_Click"
                    Background="#4F6BED"
                    Foreground="White"
                    BorderThickness="0"
                    Padding="0,8"
                    FontWeight="SemiBold"/>
        </StackPanel>
    </Grid>
</Window>
