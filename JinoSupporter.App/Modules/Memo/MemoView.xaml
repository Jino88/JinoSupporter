<UserControl x:Class="WorkbenchHost.Modules.Memo.MemoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="700"
             d:DesignWidth="1200">
    <UserControl.Resources>
        <Style x:Key="MemoEditingTextBoxStyle" TargetType="TextBox">
            <Setter Property="AcceptsReturn" Value="True" />
            <Setter Property="TextWrapping" Value="Wrap" />
            <EventSetter Event="PreviewKeyDown" Handler="MemoEditingTextBox_PreviewKeyDown" />
        </Style>
    </UserControl.Resources>
    <Grid Background="#F4F7FB">
        <Border Margin="20"
                Padding="18"
                Background="White"
                BorderBrush="#D5DDE8"
                BorderThickness="1"
                CornerRadius="10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="14" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <Grid Grid.Row="0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="10" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Row="0"
                               Grid.ColumnSpan="5"
                               Text="Memo"
                               FontSize="24"
                               FontWeight="SemiBold"
                               Foreground="#1F2937" />

                    <TextBlock Grid.Row="2"
                            Grid.ColumnSpan="2"
                            VerticalAlignment="Center"
                            Text="Memo data uses the global settings DB path."
                            Foreground="#667085" />

                    <Button x:Name="AddRowButton"
                            Grid.Row="2"
                            Grid.Column="2"
                            Margin="0,0,8,0"
                            Padding="14,6"
                            Content="Add Row"
                            Click="AddRowButton_Click" />

                    <Button x:Name="DeleteRowButton"
                            Grid.Row="2"
                            Grid.Column="3"
                            Margin="0,0,8,0"
                            Padding="16,6"
                            Content="Delete Row"
                            Click="DeleteRowButton_Click" />

                    <Button x:Name="SaveButton"
                            Grid.Row="2"
                            Grid.Column="4"
                            Padding="16,6"
                            Content="Save"
                            Click="SaveButton_Click" />
                </Grid>

                <Grid Grid.Row="2">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <DataGrid x:Name="MemoDataGrid"
                              Grid.Row="0"
                              AutoGenerateColumns="False"
                              CanUserAddRows="False"
                              CanUserDeleteRows="True"
                              HeadersVisibility="Column"
                              RowHeaderWidth="28"
                              RowHeight="92"
                              SelectionUnit="CellOrRowHeader"
                              SelectionMode="Extended"
                              GridLinesVisibility="All">
                        <DataGrid.Columns>
                            <DataGridTemplateColumn Header="" Width="42" IsReadOnly="True">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Width="26"
                                                Height="26"
                                                Padding="0"
                                                FontSize="14"
                                                FontWeight="SemiBold"
                                                Content="▶"
                                                Click="SelectRowButton_Click" />
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTextColumn Header="Date"
                                                Binding="{Binding MemoDate, UpdateSourceTrigger=PropertyChanged}"
                                                EditingElementStyle="{StaticResource MemoEditingTextBoxStyle}"
                                                Width="140" />
                            <DataGridTextColumn Header="Model"
                                                Binding="{Binding Model, UpdateSourceTrigger=PropertyChanged}"
                                                EditingElementStyle="{StaticResource MemoEditingTextBoxStyle}"
                                                Width="160" />
                            <DataGridTextColumn Header="Category"
                                                Binding="{Binding Category, UpdateSourceTrigger=PropertyChanged}"
                                                EditingElementStyle="{StaticResource MemoEditingTextBoxStyle}"
                                                Width="160" />
                            <DataGridTemplateColumn Header="Tags" Width="220">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <ItemsControl ItemsSource="{Binding TagItems}">
                                            <ItemsControl.ItemsPanel>
                                                <ItemsPanelTemplate>
                                                    <WrapPanel />
                                                </ItemsPanelTemplate>
                                            </ItemsControl.ItemsPanel>
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Button Margin="0,0,6,6"
                                                            Padding="8,3"
                                                            Background="#EEF4FF"
                                                            BorderBrush="#B2CCFF"
                                                            BorderThickness="1"
                                                            Foreground="#1D4ED8"
                                                            Content="{Binding}"
                                                            Click="TagButton_Click" />
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                                <DataGridTemplateColumn.CellEditingTemplate>
                                    <DataTemplate>
                                        <TextBox Text="{Binding Tags, UpdateSourceTrigger=PropertyChanged}"
                                                 Style="{StaticResource MemoEditingTextBoxStyle}" />
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellEditingTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTextColumn Header="Issue"
                                                Binding="{Binding Issue, UpdateSourceTrigger=PropertyChanged}"
                                                EditingElementStyle="{StaticResource MemoEditingTextBoxStyle}"
                                                Width="*" />
                            <DataGridTemplateColumn Header="Image" Width="300">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <ScrollViewer HorizontalScrollBarVisibility="Auto"
                                                      VerticalScrollBarVisibility="Disabled">
                                            <ItemsControl ItemsSource="{Binding Images}">
                                                <ItemsControl.ItemsPanel>
                                                    <ItemsPanelTemplate>
                                                        <StackPanel Orientation="Horizontal" />
                                                    </ItemsPanelTemplate>
                                                </ItemsControl.ItemsPanel>
                                                <ItemsControl.ItemTemplate>
                                                    <DataTemplate>
                                                        <Border Margin="0,0,8,0"
                                                                Padding="2"
                                                                BorderBrush="#D0D5DD"
                                                                BorderThickness="1"
                                                                CornerRadius="4"
                                                                Background="#F8FAFC">
                                                            <Button Padding="0"
                                                                    BorderThickness="0"
                                                                    Background="Transparent"
                                                                    Click="ImagePreviewButton_Click">
                                                                <Button.ContextMenu>
                                                                    <ContextMenu>
                                                                        <MenuItem Header="Delete Image"
                                                                                  Click="DeleteImageMenuItem_Click" />
                                                                    </ContextMenu>
                                                                </Button.ContextMenu>
                                                                <Image Width="72"
                                                                       Height="72"
                                                                       Stretch="UniformToFill"
                                                                       Source="{Binding Preview}" />
                                                            </Button>
                                                        </Border>
                                                    </DataTemplate>
                                                </ItemsControl.ItemTemplate>
                                            </ItemsControl>
                                        </ScrollViewer>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTemplateColumn Header="Image Action" Width="120">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Content="Paste Image"
                                                Padding="8,4"
                                                Click="PasteImageButton_Click" />
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>

                    <TextBlock x:Name="StatusTextBlock"
                               Grid.Row="1"
                               Margin="0,10,0,0"
                               Foreground="#667085"
                               Text="DB 경로를 입력하고 메모를 작성한 뒤 Save를 누르세요." />
                </Grid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
