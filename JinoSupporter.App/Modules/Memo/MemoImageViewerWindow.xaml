<Window x:Class="WorkbenchHost.Modules.Memo.MemoImageViewerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:app="clr-namespace:JinoSupporter.Controls;assembly=JinoSupporter.Controls"
        Title="Memo Image"
        WindowState="Maximized"
        WindowStartupLocation="CenterOwner"
        Background="White">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="16,16,16,0" LastChildFill="False">
            <app:AppButton HorizontalAlignment="Right"
                    Padding="14,8"
                    Content="Close"
                    Click="CloseButton_Click" />
        </DockPanel>

        <Border Grid.Row="1"
                Margin="20"
                Background="White"
                ClipToBounds="True">
            <Grid x:Name="ImageViewport"
                  Background="Transparent"
                  MouseWheel="ImageViewport_MouseWheel"
                  MouseLeftButtonUp="ImageViewport_MouseLeftButtonUp"
                  MouseLeftButtonDown="ImageViewport_MouseLeftButtonDown"
                  MouseMove="ImageViewport_MouseMove">
                <Image x:Name="PreviewImage"
                       Stretch="Uniform"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       RenderTransformOrigin="0.5,0.5">
                    <Image.RenderTransform>
                        <TransformGroup>
                            <ScaleTransform x:Name="ImageScaleTransform" ScaleX="1" ScaleY="1" />
                            <TranslateTransform x:Name="ImageTranslateTransform" X="0" Y="0" />
                        </TransformGroup>
                    </Image.RenderTransform>
                </Image>
            </Grid>
        </Border>
    </Grid>
</Window>
